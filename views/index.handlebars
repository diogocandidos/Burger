
    <div class="container-fluid">

    <img class="bglogo" src="../assets/img/50264e93aa486b4e54e140e6c8fb973a.png" alt="Burger Logo">


    <h1>Eat-Da-Burger!</h1>

   
        <div class="row">
                <div class= "col-6">
                <h2>Burger List!</h2>
                
                {{#each burgers}}
                    {{#unless devoured}}
                    {{> burger-block devour=false}} 
                    {{/unless}}
                {{/each}}
                
            </div>
            <div class= "col-6">
                <h2>Devoured!</h2>
                
                {{#each burgers}}
                    {{#if devoured}}
                    {{> burger-block devour=true}}
                    {{/if}}
                {{/each}}
                
            </div>
        </div>
   <br></br>

        <form class="create-form">
            <div class="form-group">
                <label for="burger">Burger Name:</label>
                <input type="text" id="burger" name="name"> 
                <button class = "btn" type="submit">Add Burger</button>
            </div>
        </form> 
 </div>
   <br></br>


